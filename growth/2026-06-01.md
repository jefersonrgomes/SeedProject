---
tags: [growth, dia-4, melhoria, evolua, design, 3d, parallax]
relacionado: "[[growth/2026-05-31]], [[conhecimento/design-imersivo-3d]], [[README]]"
---
# Relatório de Crescimento — Dia 4 — 2026-06-01

> Anterior: [[growth/2026-05-31]] | Próximo: growth/2026-06-02

## Ações do dia: 16 de 50

Origem: adubo do jardineiro com 4 referências de design imersivo
(`peachweb.io`, `threlte.xyz`, `theatrejs.com`, `spline.design`) + comandos
`melhoria a partir destes novos recursos` e `evolua`.

Como o projeto é vanilla JS sem bundler, **não importamos as libs** —
extraímos os princípios e aplicamos com CSS + Motion. (Sessão iniciada pelo
jardineiro, de forma interativa.)

---

## ① ATIVAR — Processar adubo

- Lido `adubo/adubo.md` → criado [[conhecimento/design-imersivo-3d]] mapeando
  cada referência ao princípio aplicável e à tradução vanilla.
- `adubo/adubo.md` limpo.

## ② CRESCER / ③ EVOLUIR — Design imersivo (sem tocar no canvas-logic)

Escopo `melhorar`: **nada** em `drawSprout`, `CANVAS_STAGE`, áudio, chat ou clima.

### 1. Tilt 3D do canvas (spline / threlte)
- `perspective: 1400px` em `#v-semente .stage`.
- `pointermove` no hero aplica `rotateX/rotateY` (máx ±6°) ao `.canvas-wrap`
  via `requestAnimationFrame`. `transition: transform 0.3s` dá retorno suave
  e um "follow" amortecido. Sem conflito: no hero o wrap usa `aura-breath`
  (só box-shadow), então o `transform` estava livre.

### 2. Spotlight ambiente segue o cursor (peachweb)
- `#v-semente` ganhou uma 2ª camada de `radial-gradient` posicionada em
  `var(--mx) var(--my)`, atualizadas no `pointermove`. Luz cinematográfica viva.
- `pointerleave` reseta as variáveis → volta ao brilho central.

### 3. Sheen / brilho especular (spline: material com luz)
- Overlay `.canvas-sheen` (`mix-blend-mode: screen`, `pointer-events:none`)
  dentro do `.canvas-wrap`. Radial branco de baixa opacidade segue o cursor
  (`--sx/--sy`). Aparece só no hover. Dá "feel" 3D ao pixel art 2D.

### 4. Revelação coreografada das seções (theatre.js)
- No `IntersectionObserver`, ao entrar uma seção não-hero, o `.section-title`
  é revelado com `opacity + y + letterSpacing` via Motion.
- **Sem `opacity:0` no CSS** — é enhancement: se o Motion (CDN) falhar, o título
  continua visível. (Lição de regressão respeitada — ver [[feedback_testing]].)

### 5. Acessibilidade
- `@media (prefers-reduced-motion: reduce)`: desliga tilt, float e sheen.
- O JS também checa `matchMedia('(prefers-reduced-motion: reduce)')` e pula o tilt.

## ④ REVISAR
- Reuso das CSS vars existentes (`--ease`, `--glow-g`) — nenhuma cor nova hardcoded.
- Verificado: `transform` no wrap não colide com `aura-breath` (box-shadow apenas).

## ⑤ DOCUMENTAR
- Este relatório + `conhecimento/design-imersivo-3d.md`.
- Lista de crescimento no site: "Canvas hero fullscreen" → ✓,
  novo item ativo "Profundidade 3D · tilt + parallax".

## ⑥ PUBLICAR
- `node --check` no script inline → **SYNTAX OK**.
- Commit + push.

---

## Decisões arquiteturais

**Por que princípios e não as libs (threlte/theatre/spline)?**
Threlte é Svelte+Three.js, Theatre.js e Spline pedem build/npm. O projeto serve
`src/` direto no GitHub Pages sem bundler. Importar qualquer uma quebraria a
arquitetura "single-file, zero build". O valor das referências é o *vocabulário
de design* (profundidade, câmera, sequência, material) — e isso se traduz em
`perspective` + `transform` + CSS vars + Motion, que já temos.

**Por que tilt com `transition` amortecido em vez de seguir 1:1?**
Seguir o cursor 1:1 parece nervoso. Uma transição de 0.3s cria um movimento
amortecido (damped), que lê como "peso" e profundidade — mais próximo de um
objeto físico 3D do que de um overlay reativo.

**Por que o reveal coreografado não usa `opacity:0` no CSS?**
Já tivemos o bug de conteúdo invisível quando o CDN do Motion falhou. Reveal é
camada extra, nunca pré-requisito de visibilidade. Default = visível; Motion só
enriquece a entrada.

---

## Estado do mundo ao final do Dia 4

```
Hero:    canvas com tilt 3D + spotlight que segue o cursor + sheen especular
Seções:  título revela coreografado ao entrar no viewport
A11y:    prefers-reduced-motion respeitado (CSS + JS)
Canvas:  stage 6 inalterado (drawSprout/áudio/chat/clima intocados)
Adubo:   limpo ✓
Conhecimento: +design-imersivo-3d
```

---

## Iteração 2 — Floração: CANVAS_STAGE 6 → 7 (CRESCER)

Comando `siga` do jardineiro → executar o próximo passo do roadmap. Esta é uma
ação `/crescer-canvas` (avança o stage), distinta do `melhorar`.

### Pixel art do botão floral (`drawSprout` stage >= 7)
- Caule sobe para y=13–12, conectando ao meristema do stage 6.
- **Sépalas verdes** (`P.sepal #4a8020`) em y=11 envolvendo a base.
- **Botão** (pétalas fechadas, `P.bud #c87890` rosa) em y=10–11.
- **Ponta abrindo** (`P.budHi #e8a8c0`) em y=8–9, com highlight.
- Pulso de "respiração" da flor: `0.7 + sin(t*0.02)*0.12`.
- Vento (`wo7`) reusa a mesma fórmula senoidal do stage 6 → topo balança ~1px,
  contínuo com o caule. Amplitude `(26-y)*0.06`.

### Wiring completo (evitar regressão)
- `CANVAS_STAGE = 7` + comentário do mapa de stages atualizado.
- `renderScene` ganhou snapshot estático do stage 7 (thumbnails/replay).
- `FRAMES`: stage 6 vira "dia 2"; novo frame "hoje · 01 jun · botão floral".
- `replayEvolution`: `stages=[0..7]`.
- Paleta `P`: +`sepal/bud/budHi`.
- Site: stat "🌸 flor: botão", label do canvas, progress 82%→88%,
  lista de crescimento (+"Botão floral · stage 7"), lista "próximo" atualizada.

### Cores da flor — decisão
Introduzi rosa (`#c87890/#e8a8c0`) como cor da floração. Harmoniza com o coração
(`#e06080`, já no mundo) e cria contraste com o verde dominante — a flor é a
"recompensa" visual do crescimento. Sépalas verdes mantêm a transição natural.

### Verificação
- `node --check` no script inline → **SYNTAX OK**.
- Coords y=8–13 dentro de 0–47; `cx`, `t`, `P.*`, `wo7` todos em escopo.
- drawSprout stages 0–6 **inalterados** (só adição de bloco `>= 7`).

---

## Estado do mundo ao final do Dia 4 (atualizado)

```
Canvas:  stage 7 · botão floral rosa balançando no ápice do caule
Hero:    tilt 3D + spotlight + sheen (Iteração 1)
Seções:  reveal coreografado no scroll
A11y:    prefers-reduced-motion respeitado
Adubo:   limpo ✓
```

## Próximo passo (Dia 5)

- Flor aberta — pétalas em leque (stage 8), a partir do botão
- Fruto — pixel art arredondado (stage 9)
- Polinização — abelha desviar trajetória para visitar a flor
- Considerar: parallax sutil das camadas de fundo (`body::before`) no scroll
- Testar no browser antes de commitar
